# NFR Requirements — Unit 2: Event & Category

**Same as Unit 1 Foundation NFRs** — performance, security, reliability targets unchanged.

**Unit-specific**: Event/Category CRUD endpoints < 200ms p95, standard input validation via Pydantic, auth required on all endpoints.
